$("#name_field").val("<%= j @recipe.name %>");
$("#ingredient_area").val("<%= j @recipe.ingredients %>");


// with j render you have to use a partial or a new view. if you just want to render text just use j