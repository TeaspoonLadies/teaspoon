<div id="content">
		<!-- <textarea id="text_area" rows="10" cols="70"></textarea> -->
		<%= form_for :shopping_list, html: { id:'shopping_list_form' } do |f| %>
			<p>
				<%= f.label :content %></br>
				<%= f.text_area :content, rows: 10, cols: 70, id: 'text_area'%>
			</p>
		<% end %>	
	</div>